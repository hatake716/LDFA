<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="internalOnly">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
      The Android-shared-storage feature is retired: at targetSdk 35 the classic
      READ/WRITE_EXTERNAL_STORAGE permissions are not user-grantable (the system
      permissions page doesn't even list them), so the /mnt/android share could
      never work for real users and its UI has been removed. Strip ALL storage
      permissions from the MERGED manifest — the bundled termux-runtime library
      and the vendor termux-app manifest still declare them (do NOT edit the git
      submodules); MANAGE_EXTERNAL_STORAGE is additionally a Play
      policy-restricted permission that Play rejects when declared but unused.
      Verify after a build that no *_EXTERNAL_STORAGE remains in
      build/intermediates/merged_manifest/.../AndroidManifest.xml.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".LinuxDesktopApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LinuxDesktop"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:allowBackup,android:icon,android:label,android:roundIcon,android:supportsRtl,android:theme,android:networkSecurityConfig">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".display.VncFallbackActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="unspecified"
            android:taskAffinity="com.termux.ldfa.compatibilityDisplay"
            android:theme="@style/Theme.LinuxDesktop" />

        <service
            android:name=".data.TermuxResultService"
            android:exported="false" />

        <service
            android:name=".x11.EmbeddedX11ServerService"
            android:exported="false"
            android:process=":x11"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Runs a local X11/Xorg display server in a dedicated process to render an on-device Linux (Debian XFCE) desktop the user explicitly starts; no standard FGS type applies (no screen capture/MediaProjection, no external/companion device, no data sync)." />
        </service>

        <service
            android:name=".service.DesktopKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the user-started on-device Linux desktop session (Xorg + terminal runtime) alive and repairs it while in use; no standard FGS type (dataSync/mediaProjection/connectedDevice) matches a persistent local compute session." />
        </service>

        <service
            android:name=".service.BackupService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
