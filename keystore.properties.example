# Play アップロード署名の設定テンプレート。
# コピーして repo ルートに keystore.properties として置く（gitignore 済み — コミット禁止）。
# キーストア本体はリポジトリの外に置くこと。生成手順は tools/release/README.md を参照。
storeFile=/home/<user>/keystores/ldfa-upload-key.jks
storePassword=<store-password>
keyAlias=ldfa-upload
keyPassword=<key-password>
