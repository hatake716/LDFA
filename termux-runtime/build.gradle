plugins {
    id 'com.android.library'
}

ext {
    packageVariant = System.getenv('TERMUX_PACKAGE_VARIANT') ?: 'apt-android-7'
}

android {
    namespace 'com.termux'
    compileSdkVersion project.properties.compileSdkVersion.toInteger()
    ndkVersion project.properties.ndkVersion

    defaultConfig {
        ndk { abiFilters 'arm64-v8a', 'x86_64' }
        minSdkVersion project.properties.minSdkVersion.toInteger()
        targetSdkVersion project.properties.targetSdkVersion.toInteger()
        buildConfigField 'String', 'TERMUX_PACKAGE_VARIANT', '"' + project.ext.packageVariant + '"'

        externalNativeBuild {
            ndkBuild {
                cFlags '-std=c11', '-Wall', '-Wextra', '-Werror', '-Os', '-fno-stack-protector', '-Wl,--gc-sections'
            }
        }
    }

    sourceSets {
        main {
            java.srcDirs = [
                '../vendor/termux-app/app/src/main/java',
                'src/main/java'
            ]
            res.srcDirs = [
                '../vendor/termux-app/app/src/main/res',
                'src/main/res'
            ]
            assets.srcDirs = [
                '../vendor/termux-app/app/src/main/assets',
                'src/main/assets'
            ]
            manifest.srcFile 'src/main/AndroidManifest.xml'
        }
    }

    externalNativeBuild {
        ndkBuild {
            path file('../vendor/termux-app/app/src/main/cpp/Android.mk')
        }
    }

    compileOptions {
        coreLibraryDesugaringEnabled true
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    buildFeatures {
        buildConfig true
    }

    packagingOptions {
        jniLibs {
            useLegacyPackaging true
        }
    }

    lint {
        // These checks target newer AndroidX activity conventions, while the
        // pinned upstream Termux activity intentionally owns its back-stack
        // handling and protected-permission declarations.
        disable 'ProtectedPermissions', 'MissingSuperCall'
    }
}

dependencies {
    implementation 'androidx.annotation:annotation:1.9.0'
    implementation 'androidx.core:core:1.13.1'
    implementation 'androidx.drawerlayout:drawerlayout:1.2.0'
    implementation 'androidx.preference:preference:1.2.1'
    implementation 'androidx.viewpager:viewpager:1.0.0'
    implementation 'com.google.android.material:material:1.12.0'
    implementation 'com.google.guava:guava:24.1-jre'
    implementation "io.noties.markwon:core:${markwonVersion}"
    implementation "io.noties.markwon:ext-strikethrough:${markwonVersion}"
    implementation "io.noties.markwon:linkify:${markwonVersion}"
    implementation "io.noties.markwon:recycler:${markwonVersion}"
    implementation 'com.google.guava:listenablefuture:9999.0-empty-to-avoid-conflict-with-guava'

    implementation project(':terminal-view')
    implementation project(':termux-shared')

    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.1.5'
}

// Both supported runtimes are built for this application's private prefix.
// Never replace them with upstream com.termux binaries on a missing file or hash mismatch.
tasks.register('verifyBootstraps') {
    doLast {
        if (project.ext.packageVariant != 'apt-android-7') {
            throw new GradleException("Unsupported TERMUX_PACKAGE_VARIANT: ${project.ext.packageVariant}")
        }
        def checksums = [
            aarch64: '224a8356e01d77b34d2e67ca03c8d464031b3f1acc16109efadd86d7d1296959',
            x86_64: 'f2bc3c6931d2f3f3502afb83062d3b2032562ec82bef76822bc76ea7ee0fa7c6',
        ]
        checksums.each { arch, expected ->
            def archive = file("../vendor/termux-app/app/src/main/cpp/bootstrap-${arch}.zip")
            if (!archive.isFile()) {
                throw new GradleException("Missing LDFA bootstrap: ${archive}. Restore the tracked file or follow tools/bootstrap/README.md.")
            }
            def digest = java.security.MessageDigest.getInstance('SHA-256')
            archive.withInputStream { input ->
                byte[] buffer = new byte[32768]
                int count
                while ((count = input.read(buffer)) >= 0) digest.update(buffer, 0, count)
            }
            def actual = new BigInteger(1, digest.digest()).toString(16).padLeft(64, '0')
            if (actual != expected) {
                throw new GradleException("LDFA bootstrap checksum mismatch for ${arch}: expected ${expected}, actual ${actual}. The local file has been preserved.")
            }
        }
    }
}

tasks.named('preBuild').configure { dependsOn(tasks.named('verifyBootstraps')) }
