<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <!-- Must live in OUR package namespace: Android refuses to install a second
         package redeclaring a permission owned by a differently-signed app, so a
         com.termux-named declaration made LDFA uninstallable next to real Termux
         (INSTALL_FAILED_DUPLICATE_PERMISSION). Vestigial in the embedded build
         (both services are exported=false and unguarded) but kept for vendor
         code that references holding it; ${applicationId} tracks any rename. -->
    <permission
        android:name="${applicationId}.permission.RUN_COMMAND"
        android:description="@string/permission_run_command_description"
        android:label="@string/permission_run_command_label"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <activity
            android:name="com.termux.app.TermuxActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|density|screenLayout|keyboard|keyboardHidden|navigation"
            android:label="@string/application_name"
            android:launchMode="singleTask"
            android:resizeableActivity="true"
            android:theme="@style/Theme.TermuxActivity.DayNight.NoActionBar" />

        <activity
            android:name="com.termux.app.activities.HelpActivity"
            android:exported="false"
            android:label="@string/application_name"
            android:parentActivityName="com.termux.app.TermuxActivity"
            android:resizeableActivity="true" />

        <activity
            android:name="com.termux.app.activities.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_termux_settings"
            android:theme="@style/Theme.TermuxApp.DayNight.NoActionBar" />

        <activity
            android:name="com.termux.shared.activities.ReportActivity"
            android:exported="false"
            android:documentLaunchMode="intoExisting"
            android:theme="@style/Theme.MarkdownViewActivity.DayNight" />

        <activity
            android:name="com.termux.app.api.file.FileReceiverActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:resizeableActivity="true"
            android:taskAffinity="com.termux.filereceiver" />

        <!--
          ContentProvider authorities must be globally unique on a device. Deriving
          them from ${applicationId} (a) avoids an install conflict with the real
          Termux app, which owns "com.termux.documents"/"com.termux.files", and
          (b) keeps them in sync with TermuxConstants.TERMUX_FILE_SHARE_URI_AUTHORITY,
          which is now "<applicationId>.files".
        -->
        <provider
            android:name="com.termux.filepicker.TermuxDocumentsProvider"
            android:authorities="${applicationId}.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <provider
            android:name="com.termux.app.TermuxOpenReceiver$ContentProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true" />

        <receiver
            android:name="com.termux.app.TermuxOpenReceiver"
            android:exported="false" />

        <receiver
            android:name="com.termux.app.event.SystemEventReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.termux.shared.activities.ReportActivity$ReportActivityBroadcastReceiver"
            android:exported="false" />

        <service
            android:name="com.termux.app.TermuxService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Provides terminal session management and execution for the Linux environment." />
        </service>

        <service
            android:name="com.termux.app.RunCommandService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Allows the main application to execute administrative scripts and X11 setup in the Termux environment." />
        </service>

        <meta-data android:name="android.max_aspect" android:value="10.0" />
        <meta-data android:name="com.samsung.android.keepalive.density" android:value="true" />
        <meta-data android:name="com.samsung.android.multidisplay.keep_process_alive" android:value="true" />
        <meta-data android:name="com.sec.android.support.multiwindow" android:value="true" />
    </application>
</manifest>
